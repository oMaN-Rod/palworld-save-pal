name: Build Desktop (Rust)

on:
  workflow_dispatch:

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            bundles: msi,nsis
            artifact_name: desktop-windows
            artifact_paths: |
              palstudio/target/release/bundle/msi/*.msi
              palstudio/target/release/bundle/nsis/*.exe
          - os: macos-latest
            bundles: dmg
            artifact_name: desktop-macos
            artifact_paths: |
              palstudio/target/release/bundle/dmg/*.dmg
          - os: ubuntu-22.04
            bundles: deb,rpm,appimage
            artifact_name: desktop-linux
            artifact_paths: |
              palstudio/target/release/bundle/deb/*.deb
              palstudio/target/release/bundle/rpm/*.rpm
              palstudio/target/release/bundle/appimage/*.AppImage
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout palstudio
        uses: actions/checkout@v3
        with:
          path: palstudio

      - name: Install Linux system dependencies
        if: matrix.os == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev build-essential curl wget file libxdo-dev libssl-dev libayatana-appindicator3-dev librsvg2-dev

      - name: Set up Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install UI deps
        working-directory: palstudio/ps-ui
        shell: bash
        run: bun install
        # The frontend itself is built by Tauri's beforeBuildCommand
        # (bun run build:desktop) during "Build Tauri bundles".

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust build
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: palstudio
          prefix-key: "v0-rust-${{ matrix.os }}"

      - name: ps-app stays transport-free
        working-directory: palstudio
        shell: bash
        run: |
          if ! tree_output=$(cargo tree -p ps-app -e normal); then
            echo "::error::cargo tree failed to run for ps-app; dependency guard could not verify anything"
            exit 1
          fi
          if echo "$tree_output" | grep -E "axum|rfd|opener|bollard|sysinfo|reqwest|tower-http|clap"; then
            echo "::error::forbidden native dependency in ps-app tree"
            exit 1
          fi

      - name: Install Tauri CLI
        uses: taiki-e/install-action@v2
        with:
          tool: tauri-cli@2

      - name: Build Tauri bundles
        working-directory: palstudio/ps-desktop
        run: cargo tauri build --bundles ${{ matrix.bundles }}

      - name: Strip host-graphics libs from AppImage
        if: matrix.os == 'ubuntu-22.04'
        working-directory: palstudio
        run: |
          appimage=$(ls target/release/bundle/appimage/*.AppImage | head -n 1)
          bash scripts/appimage-strip-graphics.sh "$appimage"

      - name: Upload installers
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: ${{ matrix.artifact_paths }}
          if-no-files-found: error
