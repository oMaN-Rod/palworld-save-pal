// Default Pal Preset Config
// Adjust the properties and types as needed to match your PalPresetConfig

import type { PalPresetConfig } from './palPresetNameDescriptionMap';

export const defaultPresetConfig: PalPresetConfig = {
  lock: false,
  lock_element: false,
  some_other_property: false
  // Add all relevant properties with their default values
};
