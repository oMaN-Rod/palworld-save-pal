## Goal
Bring `theme-overhaul` up to date with oMaN-Rod's `main` (194 commits ahead of the shared merge base), keep the PSP theme as the styling layer for all new upstream UI, verify, then land it in oMaN-Rod's main via PR #343 as a merge commit.

## Facts (already verified)
- Remotes: `origin` = CyrixJD115 fork, `upstream` = oMaN-Rod repo. PR #343 (draft, currently CONFLICTING): fork `theme-overhaul` (3 commits: `e1716005`, `0aee73e3`, `202ab83d`) → oMaN-Rod `main`.
- Merge base = `0d99b04a` (oMan's v1.2.0 merge). upstream/main tip `a1f02dbf` = 194 commits ahead; theme = 3 ahead. Local `main` is a strict ancestor of upstream/main.
- Theme architecture: Skeleton `[data-theme]` palettes + glow tokens (color-mix) + shared layout in `ui/src/app.css`. Re-theming = swap hardcoded hex/light-only styles for palette tokens.
- Upstream brings: web/wasm port, OPFS persistence, Adventure Atlas landing, MapLibre migration, blueprints, DbDriver refactor, awakened/imported pals, GPL-3.0 relicensing, `ci.yml`/`deploy-web.yml`.
- Conflict zones: `+page.svelte` (landing, both rewrote), `NavBar.svelte`/`navItems.ts`, `+layout.svelte`, `app.css`, `ups/+page.svelte` + `UPSStatsPanel.svelte`; low-content: `about`, `edit/+layout`, `file/+page`. The other ~16 theme-touched files apply cleanly (upstream didn't touch them since the base).

## Steps

### 1. Sync refs
- `git fetch upstream origin` (upstream/main may have moved past `a1f02dbf` — fetch fresh).
- Fast-forward local `main` to `upstream/main`.

### 2. Merge upstream/main into theme-overhaul (merge commit; theme commits stay intact)
On `theme-overhaul`: `git merge upstream/main`, then resolve per file:
- **Non-UI upstream work** (Rust crates, wasm, data JSON, workflows, LICENSE/GPL-3.0): take upstream wholesale — no theme overlap.
- **`+page.svelte`**: take upstream's Adventure Atlas landing (theme's restyle of the old landing is superseded); re-skin in step 3.
- **`NavBar.svelte` / `navItems.ts`**: keep theme's grouped sidebar structure; re-insert upstream's new entries (Blueprints, web gates) into the theme groups.
- **`+layout.svelte`**: theme markup/classes + upstream logic (landing gate, compat banner, isWebBuild).
- **`app.css`**: theme palette/base wins; re-apply upstream's nav-rail tweak (`8d8ef138`) on top.
- **UPS files**: themed panels + upstream's awakened/imported filter bar, stat counters, badges.
- **`about/+page.svelte`**: keep theme's Saitama easter egg (both commits), keep upstream content.
- **Criss-cross merges of #319/#326**: git handles via base `0d99b04a`; any spurious conflicts in `edit/+layout.svelte`/`file/+page.svelte` resolved as v1.2.0 content + theme styling.
- Remaining ~16 themed files (StatusBadge, Progress, Spinner, Card, Input, MissionDetails, TrustEditModal, WorkSuitabilities, SectionHeader, app.html, button.styles.ts, technologies, wiki active-skills, UPSCollections/UPSTags/UPSPalList, layout/index.ts): theme side applies cleanly.

### 3. Wrap pass — re-theme new upstream UI to PSP palette (separate commit, keeps PR diff readable)
- **Landing**: re-skin Hero/HeroMap, Features, HowItWorks, Faq, Values, DesktopApp, Cta, Link, SectionGlow, GithubLink, NexusModsLink, Logo → dark PSP surfaces, glow tokens, themed logo variant, gradient titles. Swap hardcoded hex → palette tokens.
- **CompatBanner / UnsupportedBrowser / SaveDropzone**: palette tokens.
- **Blueprints UI + map overlays/controls**: kill light-mode leaks (bg-white/bg-black) like the theme did before; tokens where hardcoded.
- **UPS awakened/imported UI** (filter bar, gold glow for awakened, imported badge): themed.
- **Nav**: Blueprints entry placed in theme's groups.

### 4. Verify
- `bun install`; `bun run check` (svelte-check); `bun run test:unit`; `bun run lint`; `bun run build` (web).
- Browser smoke-test (browser-use skill): landing, about (easter egg), UPS (awakened filters), blueprints, files page, theme switching. Fix fallout.

### 5. Push + PR
- `git push origin theme-overhaul` → PR #343 recomputes as mergeable (draft → ready).

### 6. Land in oMaN-Rod main
- Merge PR #343 with a merge commit ("Merge pull request #343 from CyrixJD115/theme-overhaul"), matching oMan's existing history. This is the irreversible outward step — I'll confirm with you right before executing it.

## Commit structure
1. Merge commit: upstream/main into theme-overhaul (conflict resolution).
2. Wrap commit(s): re-theme new upstream UI to PSP palette.

## Notes
- Keep upstream's GPL-3.0 relicensing.
- No Rust changes needed (theme touched only `ui/`).
- Playwright E2E skipped unless you want it — unit + check + build + browser smoke cover the wrap.
